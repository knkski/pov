To run, double tap the the reset button on the feather m0 while it's plugged in
via USB, which will then present a FEATHERBOOT drive. Then run `./flash` to
build the project and flash over the new bootloader.
